---
/**
 * Logo CEMAD — fuente única de verdad.
 *
 * Renderizado vectorial del frame `logo` (node 7:4) del Figma CLINICA CEMAD.
 * 465×138 con monogram CEMAD® serif + tagline "AESTHETIC ANTI-AGING CLINIC".
 *
 * Color por defecto: dorado de marca #BD9B5F.
 * tone="white" invierte a blanco (mediante filter) para fondos oscuros.
 */
type Tone = "color" | "white" | "ink";

interface Props {
  tone?: Tone;
  /** Ancho en px (default 150). El alto se calcula proporcionalmente. */
  width?: number;
  fluid?: boolean;
  alt?: string;
  class?: string;
}

const {
  tone = "color",
  width = 150,
  fluid = false,
  alt = "CEMAD — Aesthetic Anti-Aging Clinic",
  class: className = "",
} = Astro.props;

const src =
  tone === "white" ? "/brand/cemad-logo-white.png" : "/brand/cemad-logo.png";

// Para tone="ink" aplicamos brightness(0) sobre la versión gold.
const filter =
  tone === "ink" ? "filter:brightness(0)" : undefined;

const styleAttr = [
  `width:${width}px`,
  fluid ? "max-width:100%;height:auto" : "",
  filter,
]
  .filter(Boolean)
  .join(";");
---

<img
  src={src}
  alt={alt}
  width={width}
  loading="eager"
  decoding="async"
  style={styleAttr}
  class={`block ${className}`}
/>
